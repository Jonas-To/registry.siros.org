{
  "name": "registry.siros.org",
  "version": "0.1.0",
  "description": "Static site builder for Credential Type Registry at registry.siros.org",
  "main": "scripts/build.js",
  "scripts": {
    "build": "node scripts/build.js",
    "serve": "npx http-server dist -p 8000",
    "clean": "rm -rf dist",
    "lint": "eslint scripts/",
    "test": "node --test tests/"
  },
  "keywords": [
    "vctm",
    "verifiable-credentials",
    "registry",
    "siros"
  ],
  "author": "SIROS Foundation",
  "license": "Apache-2.0",
  "dependencies": {
    "handlebars": "^4.7.8",
    "marked": "^12.0.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
